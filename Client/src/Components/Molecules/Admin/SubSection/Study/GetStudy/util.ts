import { Dispatch } from "react";

export const StudySearch = ({
  value,
  studyList,
  setStudyList,
}: {
  value: string;
  studyList: any;
  setStudyList: Dispatch<any>;
}) => {
  // setStudyList(
  //   studyList.filter(
  //     (item: { status: string }) => item.status === STUDY_STATE[value]
  //   )
  // );
};
