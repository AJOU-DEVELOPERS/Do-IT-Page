import { Dispatch } from "react";

export const ProjectSearch = ({
  value,
  projectList,
  setProjectList,
}: {
  value: string;
  projectList: any;
  setProjectList: Dispatch<any>;
}) => {
  // setProjectList(
  //   totalProjectList.filter(
  //     (item: { status: string }) => item.status === PROJECT_STATE[value]
  //   )
  // );
};
