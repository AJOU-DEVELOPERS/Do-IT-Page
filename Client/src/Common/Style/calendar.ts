import { CALENDAR_THEME } from ".";

export const CALENDAR_OPTION = {
  view: "week",
  theme: CALENDAR_THEME,
  scheduleView: true,
  taskView: true,
  useDetailPopup: true,
  useCreationPopup: true,
  usageStatistics: false,
  height: "600px",
  week: {
    daynames: ["일", "월", "화", "수", "목", "금", "토"],
  },
};
